CertWatch가 배포되었습니다! 🔒

접속 주소:
  https://{{ .Values.ingress.host }}:30122   (HTTPS)
  http://{{ .Values.ingress.host }}:30111    (HTTP)

상태 확인:
  helm list
  kubectl get pods

주의: 이 차트는 아래 리소스를 차트 밖에서 관리합니다 (먼저 생성 필요).
  - Secret: {{ .Values.secretName }}
  - ConfigMap: {{ .Values.codeConfigMap }}
